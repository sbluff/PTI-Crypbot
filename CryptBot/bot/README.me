Run with python3!

In order to interact with yout Binance API you should add a file in the API folder named binance-keys.txt following this sintax:
API_KEY: 'your public key'
SECRET_KEY='your private key'

Required libraries for running the bot:
    1) python-binance -- pip install python-binance
    2) pip install requests
    3) pip install mysql-connector-python
    4) pip install numpy
    5) pip install pandas